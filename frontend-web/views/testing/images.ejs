<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>S3 Images</title>
</head>

<body>
    <div class="container">
        <h1>Imágenes desde S3 (URLs firmadas)</h1>
        <% if (imageUrls && imageUrls.length > 0) { %>
            <div class="image-gallery">
                <% imageUrls.forEach(function(imageUrl) { %>
                    <div class="image-item">
                        <img src="<%= imageUrl %>" alt="Imagen de S3" style="max-width: 300px; height: auto;" />
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No hay imágenes disponibles en S3.</p>
        <% } %>
    </div>
</body>


</html>
